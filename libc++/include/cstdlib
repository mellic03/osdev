#pragma once

namespace std
{
    #include <stdlib.h>

    // inline int itoa( int value, char *sp, int radix )
    // {
    //     return ::itoa(value, sp, radix);
    // }

    // inline int ltoa( int64_t value, char *sp, int radix )
    // {
    //     return ::ltoa(value, sp, radix);
    // }
    
    // inline int utoa( uint32_t value, char *sp, int radix )
    // {
    //     return ::utoa(value, sp, radix);
    // }

    // inline int ultoa( uint64_t value, char *sp, int radix )
    // {
    //     return ::ultoa(value, sp, radix);
    // }
}
