#pragma once



namespace std
{
    #include <string.h>

    // inline size_t strlen( const char *str )
    // {
    //     return ::strlen(str);
    // }

    // inline int strcmp( const char *lhs, const char *rhs )
    // {
    //     return ::strcmp(lhs, rhs);
    // }

    // inline void *memcpy( void *dst, const void *src, size_t n )
    // {
    //     return ::memcpy(dst, src, n);
    // }

    // inline void *memset( void *s, uint8_t c, size_t n )
    // {
    //     return ::memset(s, c, n);
    // }

    // inline void *memmove( void *dst, const void *src, size_t n )
    // {
    //     return ::memmove(dst, src, n);
    // }

    // inline int memcmp( const void *lhs, const void *rhs, size_t n )
    // {
    //     return ::memcmp(lhs, rhs, n);
    // }

}


// void std::putc( char c )
// {
// 	ckTerminal_putc(c);
// }


// void std::puts( const char *s )
// {
//     ckTerminal_puts(s);
// }